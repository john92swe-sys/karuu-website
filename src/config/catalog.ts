/**
 * Temporary public catalogue switches.
 *
 * Keep Hydration product data and historical URLs intact while hiding the
 * category from public discovery. Flip to `true` when approved product
 * photography is ready, then rerun the catalogue and SEO regression suite.
 */
export const HYDRATION_PUBLICLY_DISCOVERABLE = false;

